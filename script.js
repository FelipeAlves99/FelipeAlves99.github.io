
/* MAKES THE LINK CLICK BE CENTRALIZED */
/*window.onload = function(linkname) {
    var hashlink = document.getElementById(linkname);

    hashlink.onclick = function(){
        let anchorheight = document.getElementById(linkname).clientHeight;
        let headerheight = document.getElementsByClassName("header").clientHeight;

        let desireheight = anchorheight - headerheight;
        
    }
}*/


/* MAKES THE HEADER HIDE */
/*let prevScrollPos = window.pageYOffset;
window.onscroll = function() {
    let currentScrollPos = window.pageYOffset;
    if(prevScrollPos > currentScrollPos) {
        document.getElementById("header").style.top = "0";
    }
    else {
        document.getElementById("header").style.top = "-85px"
    }
    prevScrollPos = currentScrollPos;
}*/

